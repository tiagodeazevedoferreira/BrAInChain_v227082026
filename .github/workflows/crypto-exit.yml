name: Crypto Exit Intelligence

on:
  workflow_dispatch:
  push:
    paths:
      - 'crypto_exit/**'
      - '.github/workflows/crypto-exit.yml'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: crypto_exit
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip pytest
      - run: python -m pytest -q
